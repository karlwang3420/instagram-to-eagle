# Store assets for 0.9.3

## Icon

Use [`icons/icon-128.png`](../../icons/icon-128.png) as the listing icon.

## Screenshots and captions

| File | Suggested caption |
| --- | --- |
| [settings.png](settings.png) | Choose an Eagle folder and control default, hashtag, and creator tags. Example library shown. |
| [setup.png](setup.png) | Check access to Instagram and Eagle on your computer before saving. Example library shown. |

Captured on September 16, 2026 using Firefox 156 and the 0.9.3 extension's actual popup DOM and CSS. The integration fixture renders that DOM on an isolated test page because Firefox BiDi cannot capture privileged extension pages. The example `References` folder and Eagle connection response are controlled test data. Images have not been retouched or generated by AI.

The interface is English. These images show settings and setup; they are not evidence of a live Instagram import. The internal screenshot `menu.png` was copied to `settings.png`; the captured UI itself displays version 0.9.3.
